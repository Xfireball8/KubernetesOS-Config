include: manifests/kubernetesos.yaml

releasever: "33"

rojig:
  license: MIT
  name: fedora-coreos
  summary: Fedora CoreOS stable

repos:
  - fedora
  - fedora-coreos-pool

add-commit-metadata:
  fedora-coreos.stream: stable
